# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ztguard
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Secret (use a long random string in production)
JWT_SECRET=ztguard_dev_secret_change_in_production_2024

# Server
PORT=3001
NODE_ENV=development

# Demo mode (uses in-memory store if no DB available)
USE_IN_MEMORY=false
